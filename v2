# Cell 1: Import Required Modules
# --------------------------------
# Import required modules for parsing and RAG
import pathlib
import sys
import os

# Add the main project folder to Python's module search path
main_project_folder = "path_to_main_project_folder"  # Replace with the absolute or relative path to your main project folder
if main_project_folder not in sys.path:
    sys.path.append(main_project_folder)

# Import functions from parse_script and rag_app
from parse_script import parse_page_v2, Granularity  # Assuming this file is in the project folder
from rag_app import call_rag  # Assuming this file is also in the project folder or a subfolder
